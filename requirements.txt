# Core workflow
langgraph>=0.2.0
langchain-core>=0.3.0

# Sandbox multi-agent (fixer + validator) loop
deepagents>=0.5.0
langchain>=1.0.0

# Data models
pydantic>=2.0.0

# Optional LLM providers (only needed for live routing / sandbox agents).
# Install whichever matches the API key you set.
# langchain-openai>=0.2.0
# langchain-anthropic>=0.2.0
